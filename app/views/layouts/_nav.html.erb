<nav class="navbar navbar-default navbar-fixed-top topnav" role="navigation">
    <div class="container topnav">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand topnav" href="#" style="color:#FE642D; font-size: 25px; text-align: center; padding-top: 20px;">OrderOut</a>
        </div>
        <ul class="breadcrumb" style="text-align:right;">
          <% if params[:controller] == 'groups' || params[:controller] == "orders" || params[:controller] == "group_members" %>
            <li style="padding-top:10px;"><a href="/groups/">My Groups</a></li>
          <% end %>
          <% if params[:action] == 'show' %>
            <li style="padding-bottom:-15px;"><%= link_to 'Member Order', '/group_members/'+ @order.group_member.id.to_s + '/orders' %></li>
            <li>Order View</li>
          <% end %>
          <% if params[:action] == 'orders' %>
            <li style="padding-top:10px;"><%= link_to 'Group Members', '/groups/'+ @group_member.group.id.to_s %></li>
            <li><a href="#">Member Order</a></li>

          <% end %>
        </ul>
    </div>
    <!-- /.container -->
</nav>
