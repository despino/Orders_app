<body id="orders_index">
<!-- Navigation -->
<nav class="navbar navbar-default navbar-fixed-top topnav" role="navigation">
    <div class="container topnav">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand topnav" href="#" style="color:#FE642D; font-size: 25px; text-align: center; padding-top: 20px;">OrderOut</a>
        </div>
        <ul class="breadcrumb" style="text-align:right;">
          <li style="padding-top:10px;"><a href="/groups/">My Groups</a></li>
          <li style="padding-top:10px;"><%= link_to 'Group Members', '/groups/'+ @group_member.group.id.to_s %></li>
          <li>Member Order</li>
        </ul>
    </div>
    <!-- /.container -->
</nav>

<!-- <p id="notice"><%= notice %></p> -->
<div class="gray_box">

  <h1 class="index_listing">
    <%= @group_member.diner_name %>'s
    <strong>Orders</strong>
  </h1>


<div class="table-repsonsive">
  <table class="table table-striped">
  <thead>
    <tr>
      <th><strong>Restaurants</strong></th>
      <th colspan="3"></th>
    </tr>
  </thead>

    <tbody>
        <% @orders.each do |order| %>
          <tr>
              <td class="col-md-2"><%= order.Restaurant %></td>
              <td class="col-md-1"><%= link_to 'Show Order', order %></td>
              <!-- <td><%= link_to 'Edit', edit_order_path(order) %></td> -->
              <td class="col-md-1"><%= link_to 'Delete Order', order, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
    </tbody>
  </table>

  <br>

    <%= link_to 'New Order', "/group_members/" + @group_member.id.to_s + "/new_order" %> |
    <%= link_to 'Back', '/groups/'+@orders.first.group_member.group.id.to_s %> |
</div>
</body>
